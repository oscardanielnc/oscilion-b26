# --- Oscilion ---
# Datos y artefactos (nunca al repo). ANCLADOS a la raíz con "/" para NO ignorar
# paquetes fuente como oscilion/data/ (bug previo: "data/" los ocultaba).
/data/
/logs/
*.db
*.db-wal
*.db-shm
*.parquet

# Secretos / entorno
.env
*.env
!example.env

# Python (build dirs anclados a la raíz para no chocar con frontend/dist)
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
/build/
/dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Frontend
frontend/node_modules/
# frontend/dist SÍ se versiona: la VM sirve el build sin necesitar Node.

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
